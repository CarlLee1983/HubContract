{
  "id": "currency-index-unknown-station",
  "name": "Currency index unknown station",
  "description": "Records Legacy currency route index-unknown-station.",
  "route": {
    "method": "GET",
    "path": "/v1/currencies"
  },
  "tags": [
    "currency",
    "index",
    "unknown-station",
    "CAP-14",
    "CAP-01",
    "BR-01"
  ],
  "request": {
    "headers": {
      "Host": "localhost:8080",
      "Accept": "application/json"
    },
    "query": {
      "timestamp": 0,
      "station_code": "NON_EXISTENT_STATION",
      "sign": "dummy_sign_for_unknown_station"
    }
  },
  "normalizers": [
    {
      "target": "request.query.timestamp",
      "type": "current_timestamp"
    },
    {
      "target": "response.body.file",
      "type": "mask",
      "replacement": "<MASKED_FILE_PATH>"
    },
    {
      "target": "response.body.trace",
      "type": "mask",
      "replacement": "<MASKED_TRACE>"
    }
  ],
  "dbProbe": {
    "queries": [
      {
        "name": "station",
        "sql": "SELECT id, code, cost_percent FROM stations WHERE code = ? AND deleted_at IS NULL ORDER BY id",
        "params": [
          "NON_EXISTENT_STATION"
        ]
      },
      {
        "name": "station_currencies",
        "sql": "SELECT sc.id, sc.currency, sc.status FROM station_currencies sc JOIN stations s ON s.id = sc.station_id WHERE s.code = ? AND sc.deleted_at IS NULL ORDER BY sc.id",
        "params": [
          "NON_EXISTENT_STATION"
        ]
      }
    ]
  },
  "mongoProbe": {
    "pattern": "httplog_*"
  }
}
