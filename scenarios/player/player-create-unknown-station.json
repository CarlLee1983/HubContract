{
  "id": "player-create-unknown-station",
  "name": "Player Create: unknown station",
  "description": "CAP-02 / D-03: Unknown station is rejected before controller.",
  "route": {
    "method": "POST",
    "path": "/v1/player"
  },
  "tags": [
    "player",
    "create",
    "unknown-station",
    "CAP-02"
  ],
  "request": {
    "headers": {
      "Host": "localhost:8080",
      "Accept": "application/json"
    },
    "body": {
      "currency": "TWD",
      "station_code": "NON_EXISTENT_STATION",
      "timestamp": 0,
      "account": "synthetic_user_04",
      "sign": "invalid_signature_hex_value_12345"
    }
  },
  "dbProbe": {
    "queries": [
      {
        "name": "member",
        "sql": "SELECT id, station_id, account FROM users WHERE station_id = 1 AND account = ?",
        "params": [
          "synthetic_user_04"
        ]
      },
      {
        "name": "main_players",
        "sql": "SELECT id, user_id, platform_id, account FROM players WHERE platform_id = 1 AND user_id IN (SELECT id FROM users WHERE station_id = 1 AND account = ?) ORDER BY id",
        "params": [
          "synthetic_user_04"
        ]
      },
      {
        "name": "main_wallets",
        "sql": "SELECT id, user_id, platform_id, player_id, currency, balance, freeze FROM wallets WHERE platform_id = 1 AND user_id IN (SELECT id FROM users WHERE station_id = 1 AND account = ?) ORDER BY id",
        "params": [
          "synthetic_user_04"
        ]
      }
    ]
  },
  "normalizers": [
    {
      "target": "request.body.timestamp",
      "type": "current_timestamp"
    },
    {
      "target": "response.body.file",
      "type": "mask",
      "replacement": "<MASKED_FILE_PATH>"
    },
    {
      "target": "response.body.trace",
      "type": "mask",
      "replacement": "<MASKED_TRACE>"
    }
  ]
}
