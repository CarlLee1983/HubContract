{
  "id": "player-create-account-suffix-error",
  "name": "Player Create: account already ends with station code",
  "description": "CAP-02 / P-03: Characterize Legacy process() behavior when account already ends with station code.",
  "route": {
    "method": "POST",
    "path": "/v1/player"
  },
  "tags": [
    "player",
    "create",
    "account-suffix-error"
  ],
  "request": {
    "headers": {
      "Host": "localhost:8080",
      "Accept": "application/json"
    },
    "body": {
      "currency": "TWD",
      "station_code": "DEMO_STATION",
      "timestamp": 0,
      "account": "synthetic_new_DEMO_STATION"
    },
    "signWith": {
      "secretKey": "synthetic_secret_key_for_contract_testing_only_1234567890"
    }
  },
  "dbProbe": {
    "queries": [
      {
        "name": "member",
        "sql": "SELECT id, station_id, account FROM users WHERE station_id = 1 AND account = ?",
        "params": [
          "synthetic_new_DEMO_STATION"
        ]
      },
      {
        "name": "main_players",
        "sql": "SELECT id, user_id, platform_id, account FROM players WHERE platform_id = 1 AND user_id IN (SELECT id FROM users WHERE station_id = 1 AND account = ?) ORDER BY id",
        "params": [
          "synthetic_new_DEMO_STATION"
        ]
      },
      {
        "name": "main_wallets",
        "sql": "SELECT id, user_id, platform_id, player_id, currency, balance, freeze FROM wallets WHERE platform_id = 1 AND user_id IN (SELECT id FROM users WHERE station_id = 1 AND account = ?) ORDER BY id",
        "params": [
          "synthetic_new_DEMO_STATION"
        ]
      }
    ]
  },
  "normalizers": [
    {
      "target": "request.body.timestamp",
      "type": "current_timestamp"
    },
    {
      "target": "response.body.data.created_at",
      "type": "mask",
      "replacement": "<MASKED_TIMESTAMP>"
    },
    {
      "target": "response.body.file",
      "type": "mask",
      "replacement": "<MASKED_FILE_PATH>"
    },
    {
      "target": "response.body.trace",
      "type": "mask",
      "replacement": "<MASKED_TRACE>"
    }
  ]
}
