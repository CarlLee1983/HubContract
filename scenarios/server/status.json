{
  "id": "server-status",
  "name": "Server status: unauthenticated liveness response",
  "description": "Route #20 / D-36: the Legacy liveness endpoint returns OK without checking Station or dependencies.",
  "route": {
    "method": "GET",
    "path": "/v1/server/status"
  },
  "tags": [
    "server",
    "status",
    "success",
    "D-36",
    "CAP-17"
  ],
  "request": {
    "headers": {
      "Host": "localhost:8080",
      "Accept": "application/json"
    }
  },
  "dbProbe": {
    "queries": [
      {
        "name": "station",
        "sql": "SELECT id, code, cost_percent FROM stations WHERE code = ? AND deleted_at IS NULL ORDER BY id",
        "params": [
          "DEMO_STATION"
        ]
      }
    ]
  },
  "mongoProbe": {
    "pattern": "httplog_*"
  }
}
