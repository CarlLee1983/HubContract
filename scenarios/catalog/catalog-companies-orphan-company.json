{
  "id": "catalog-companies-orphan-company",
  "name": "Catalog companies: orphan company",
  "description": "D-45: resource skips the orphan station company and returns remaining rows.",
  "route": {
    "method": "GET",
    "path": "/v1/games/companies"
  },
  "tags": [
    "catalog",
    "companies",
    "orphan-company"
  ],
  "request": {
    "headers": {
      "Host": "localhost:8080",
      "Accept": "application/json"
    },
    "query": {
      "station_code": "CATALOG_ORPHAN",
      "timestamp": 0
    },
    "signWith": {
      "secretKey": "synthetic_secret_key_for_CATALOG_ORPHAN_1234567890"
    }
  },
  "normalizers": [
    {
      "target": "request.query.timestamp",
      "type": "current_timestamp"
    },
    {
      "target": "response.body.file",
      "type": "mask",
      "replacement": "<MASKED_FILE_PATH>"
    },
    {
      "target": "response.body.trace",
      "type": "mask",
      "replacement": "<MASKED_TRACE>"
    }
  ],
  "dbProbe": {
    "queries": [
      {
        "name": "station_companies",
        "sql": "SELECT sgc.id, sgc.game_company_id, sgc.game_company_name, sgc.active, sgc.currency FROM station_game_companies sgc JOIN stations s ON s.id = sgc.station_id WHERE s.code = ? AND sgc.deleted_at IS NULL ORDER BY sgc.id",
        "params": [
          "CATALOG_ORPHAN"
        ]
      }
    ]
  }
}
